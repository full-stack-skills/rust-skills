interface:
  display_name: "Rust Java Migration"
  short_description: "Audit source-authoritative Java-to-Rust migration parity"
  default_prompt: "Use $rust-java-migration to build an idiomatic Rust API, record JavaBean or script compatibility as ADAPTED through explicit registries, preserve full source semantics, and verify the complete migration batch without treating adaptation as completion."
