interface:
  display_name: "Rust Java Migration Testing"
  short_description: "Prove lossless source-test and result parity"
  default_prompt: "Use $rust-java-migration-testing to make a non-published <project>-test package own whole-project acceptance, migrate 100% of source tests/assets without loss, verify byte-identical fixtures and complete per-case Java/Rust MATCH results, then add Rust-specific tests."
