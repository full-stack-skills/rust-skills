interface:
  display_name: "Rust Cargo Build"
  short_description: "Configure Cargo builds, dependencies, and releases"
  default_prompt: "Use $rust-cargo-build to configure and validate this Cargo build or dependency change."
